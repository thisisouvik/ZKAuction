/**
 * contract/src/generated/index.cjs
 *
 * STUB FILE — replaced by compactc output when you run:
 *   npm run compile:contract
 *
 * This stub exists so Next.js can build without the compiled contract.
 * The actual compiled contract (from compactc) will export a Contract
 * object with the circuit definitions, verifier keys, and ZKIR.
 *
 * When compactc runs it will overwrite this file with the real output.
 */
module.exports = {
  // Placeholder — compactc will replace with real compiled contract
  Contract: null,
  __isStub: true,
};
