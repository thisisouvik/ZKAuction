# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — NEVER commit real secrets
.env*
!.env.example

# vercel
.vercel
IMPLEMENTATION _PLAN.md
# typescript
*.tsbuildinfo
next-env.d.ts

# ─── Midnight-specific ────────────────────────────────────────────
# Compact compiler output (auto-generated, do not commit)
# (Actually, we need to commit these for Vercel deployment unless we compile on Vercel)

# Private state database (local only, contains private keys)
midnight-private-state.db
*.leveldb/

# Docker volume data
.docker-data/

# Seller private config (reserve price + salt — NEVER commit!)
.seller-config.json

# Deployment artifacts
scripts/deploy-output.json

/lib/generated/prisma

# IDEs & Agents
.agents/
.claude/
.windsurf/
skills-lock.json

# Compiler / Public Assets
# contract/src/generated/
# public/keys/
# public/zkir/
# stubs/

